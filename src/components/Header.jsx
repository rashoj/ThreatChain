import React from "react";

const Header = () => {
    return (
        <header className="header">
            <h1 className="text-2xl">Security Dashboard</h1>

        </header>
    );
};

export default Header;